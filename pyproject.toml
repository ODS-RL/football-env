[project]
name = "football"
version = "0.1.0"
description = "Python 2D football (soccer) simulation for AI agent competition"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.5.0",
    "websockets>=12.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
]
